interface:
  display_name: "Tab Guard"
  short_description: "Repo-specific patterns and workflows for tab-guard"
  default_prompt: "Use the tab-guard repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true